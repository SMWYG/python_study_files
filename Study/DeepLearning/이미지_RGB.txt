이미지는 가로 세로 pixel 그리고 RGB 로 이루어진다.

가로x세로xR

가로x세로xG

가로x세로xB

이런식으로 0~255의 값을 가진다. 각 픽셀이

그리고 흑백 이미지의 경우

가로 X 세로 RGB(x)
즉, 총 2차원 데이터로 구성이 되는 것이다.
이때 각 데이터의 값들은 컬러였을 때의 값과는 현저히 다른 (white)0~1(black) 사이의 값을 가지게 된다. (0.~ 의 값을 가지게 됨)




pixel 값이 데이터로 구성될 때 맨좌측상단에서 시작하여 오른쪽으로 그리고 다음줄 좌측상단 오른쪽 이런식으로 진행이 된다.